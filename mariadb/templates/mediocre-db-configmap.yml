apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mariadb.fullname" . }}
  labels:
    {{- include "mariadb.labels" . | nindent 4 }}
{{- with .Values.configMapData }}
data:
  {{- toYaml . | nindent 2 }}
{{- end }}